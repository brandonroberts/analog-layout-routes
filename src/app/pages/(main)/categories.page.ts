import { Component } from "@angular/core";

@Component({
  selector: 'app-main-categories',
  standalone: true,
  template: `categories page works`
})
export default class CategoriesPageComponent { }
